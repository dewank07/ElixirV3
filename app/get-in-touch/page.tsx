import GetInTouchLayout from "@/layouts/GetInTouchLayout";
import React from "react";
import { Metadata } from "next";
export const metadata: Metadata = {
  title: "Get In Touch",
};

const page = () => {
  return <GetInTouchLayout />;
};

export default page;
